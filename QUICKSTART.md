## Quick Start

1. **Build**: `dotnet build --configuration Release`
2. **Run**: `dotnet run --configuration Release`
3. **Publish**: `dotnet publish -c Release --self-contained -r win-x64`

The executable will run natively on Windows without Python or external dependencies!

See SETUP.md for detailed hardware setup.
